# Premium Markdown Reader & Server Tasks

- [x] Create `server.py` custom Python HTTP server for dynamic markdown compilation
- [x] Modify `Makefile` to integrate the custom `server.py` startup and pkill commands
- [x] Modify `index.html` to import `marked.js`
- [x] Add premium dark-theme CSS style rules for `.markdown-body` inside `index.html`
- [x] Redesign `.doc-reader-panel` in `index.html` to add dynamic document viewport
- [x] Update `activateDoc(key)` javascript inside `index.html` to fetch and render files
- [x] Verify the server boots and serves files perfectly
- [x] Create walkthrough.md summarizing changes and verifying visual outputs
